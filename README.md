# Minesweeper
## Execution

First, assuming you are currently inside `AdvancedCPP`:

```bash
mkdir build
cd build
```

Then run:

```bash
cmake ..
```

Next, build the project:

```bash
make
```

Finally, start the game:

```bash
./minesweeper
```
